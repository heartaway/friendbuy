<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.taobao.android.taonight"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="16" />

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name" >
        <activity
            android:name=".activity.StartActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity" >
        </activity>
        <activity android:name=".activity.FriendRecommendActivity" >
        </activity>
        <activity android:name=".activity.MyRecommendActivity" >
        </activity>
        <activity android:name=".activity.AddRecommendActivity" >
        </activity>
        <activity android:name=".activity.HotRecommendActivity" >
        </activity>
        <activity android:name=".activity.MenuActivity" >
        </activity>
    </application>
    <!-- 基础信息权限 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>

</manifest>